Marks

CREATE TABLE marks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    roll_no INT(11),
    science INT,
    maths INT,
    english INT,
    total INT,
    FOREIGN KEY (roll_no) REFERENCES student(roll_no)
        ON UPDATE CASCADE
        ON DELETE CASCADE
);



